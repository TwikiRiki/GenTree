import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jules.genealogy',
  appName: 'Genealogy Tree Manager',
  webDir: 'out/renderer'
};

export default config;
